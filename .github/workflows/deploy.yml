name: Build and Deploy

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    
    steps:
      - name: 🛎️ Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: ⎔ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: 📦 Install dependencies
        run: |
          npm ci --no-audit
          echo "✅ Dependencias instaladas"

      - name: 🔍 Verificar estructura del proyecto
        run: |
          echo "=== ESTRUCTURA DEL PROYECTO ==="
          find . -type f -name "*.scss" -o -name "*.css" | sort
          echo ""
          echo "=== CONTENIDO src/scss/ ==="
          ls -la src/scss/ || echo "❌ Directorio src/scss/ no existe"
          echo ""
          echo "=== VERIFICANDO main.scss ==="
          if [ -f "src/scss/main.scss" ]; then
            echo "✅ main.scss encontrado"
            head -20 src/scss/main.scss
          else
            echo "❌ main.scss NO encontrado"
            echo "Creando archivo main.scss básico..."
            mkdir -p src/scss
            echo "/* Main SCSS File */" > src/scss/main.scss
            echo "@import 'components/profile';" >> src/scss/main.scss
            echo "@import 'components/hero';" >> src/scss/main.scss
            echo "@import 'components/footer';" >> src/scss/main.scss
            echo "@import 'components/intro';" >> src/scss/main.scss
          fi

      - name: 🎨 Compilar SCSS (Desarrollo)
        run: |
          echo "=== COMPILANDO SCSS ==="
          npx gulp styles 2>&1 || {
            echo "❌ Error durante la compilación"
            exit 1
          }

      - name: 📄 Verificar archivos generados
        run: |
          echo "=== ARCHIVOS GENERADOS ==="
          if [ -d "assets/css" ]; then
            echo "✅ Directorio assets/css existe"
            ls -la assets/css/
            echo ""
            echo "=== CONTENIDO DE main.css ==="
            if [ -f "assets/css/main.css" ]; then
              echo "✅ main.css generado correctamente"
              echo "Tamaño: $(wc -l < assets/css/main.css) líneas"
              echo "Primeras 10 líneas:"
              head -10 assets/css/main.css
            else
              echo "❌ main.css NO generado"
              exit 1
            fi
          else
            echo "❌ Directorio assets/css NO existe"
            exit 1
          fi

      - name: 📦 Crear artifact de compilación
        uses: actions/upload-artifact@v4
        with:
          name: css-build
          path: assets/css/
          retention-days: 7
        if: success()

  deploy:
    needs: build-and-test
    if: github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: 🛎️ Checkout repository
        uses: actions/checkout@v4

      - name: ⎔ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: css-build
          path: assets/css/

      - name: ✅ Verificar archivos antes del deploy
        run: |
          echo "=== ARCHIVOS PARA DEPLOY ==="
          ls -la assets/css/
          if [ ! -f "assets/css/main.css" ]; then
            echo "❌ ERROR: main.css no encontrado para deploy"
            exit 1
          fi
          echo "✅ Todo listo para deploy"

      - name: 🚀 Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./
          force_orphan: true
          exclude_assets: 'node_modules/,.github/,.git/,src/,*.md,package*.json,gulpfile.js'

      - name: 📢 Notificar despliegue
        run: |
          echo "✅ Sitio desplegado en GitHub Pages"
          echo "🌐 URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/"